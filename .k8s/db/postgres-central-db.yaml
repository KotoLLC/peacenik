---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: central-db-pvc
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: do-block-storage
---
apiVersion: v1
kind: Service
metadata:
  name: db-central-service
  namespace: backend
  labels:
    app: db-central-service
spec:
  ports:
    - port: 5432
  selector:
    app: db-central-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-central-db
  namespace: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: db-central-service
  template:
    metadata:
      labels:
        app: db-central-service
    spec:
      containers:
        - name: postgres
          image: postgres:12.2
          env:
          - name: POSTGRES_DB
            value: koto-central
          - name: POSTGRES_PASSWORD
            valueFrom:
              secretKeyRef:
                name: db-central-password
                key: password
          ports:
          - containerPort: 5432
          volumeMounts:
          - name: central-db-data
            mountPath: /var/lib/postgresql
      volumes:
      - name: central-db-data
        persistentVolumeClaim:
          claimName: central-db-pvc
---
apiVersion: v1
data:
  password: ZG9ja2Vy
kind: Secret
metadata:
  name: db-central-password
  namespace: backend
